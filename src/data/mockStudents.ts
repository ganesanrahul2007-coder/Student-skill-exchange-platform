import { UserProfile } from '../types/user';

/**
 * Clean student directory.
 * Real student profiles are created when users register on the platform.
 */
export const MOCK_STUDENTS: UserProfile[] = [];
